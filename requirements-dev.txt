# Test and benchmark tooling. Not needed to run the app.
#   py -3 -m pip install -r requirements-dev.txt
#   py -3 -m pytest
pytest>=8
# data/audit_responsive.py and data/shoot.py drive a headless browser
playwright>=1.40
